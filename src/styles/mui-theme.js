import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#FF8000',
            dark: '#FF8C1A',
        },
        secondary: {
            main: '#FFC426',
        },
        common: {
            main: '#fff',
            black: '#000',
            white: '#fff',
        },
    },
});