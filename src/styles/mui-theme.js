import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#FF8C1A',
            dark: '#FF8C1A',
        },
        secondary: {
            main: '#FFC426',
        },
        common: {
            main: '#000',
            black: '#000',
            white: '#fff',
        },
    },
});